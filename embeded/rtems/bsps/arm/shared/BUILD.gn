import("//gn/toolchain/rtems/rtems.gni")

component("shared") {
    defines = []
    sources = ["fatal.c"]
    if (use_backtrace) {
        defines += ["CONFIGURE_BACKTRACE"]
        sources += ["unwind.c"]
    }
    deps = ["//embeded/rtems/bsps/arm/shared/wind"]

}
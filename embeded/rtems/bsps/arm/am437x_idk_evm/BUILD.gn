import("//gn/toolchain/rtems/rtems.gni")

component("board") {
  deps = []
  include_dirs = [
    "include"
  ]
  sources = [
    "am43xx.c",
    "console.c",
    "gpio.c",
    "gpio_keys.c",
    "timers.c",
    "platform_resource.c"
    # "clock.c",
    # "clock_am43xx.c"
  ]
  defines = ["CONFIG_AM43XX"]
  if (use_libbsd) {
    deps += ["bsd:bsd_drivers"]
  }
}


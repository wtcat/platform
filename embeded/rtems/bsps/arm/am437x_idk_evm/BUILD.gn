import("//gn/toolchain/rtems/rtems.gni")

component("board") {
  include_dirs = [
    "include"
  ]
  sources = [
    "am43xx.c",
    "console.c",
    "gpio.c",
    "platform_resource.c"
    # "clock.c",
    # "clock_am43xx.c"
  ]
  defines = [
    "CONFIG_AM43XX"
  ]
  # deps = [
  #   "//embeded/rtems/bsps/arm/shared/lib:libc"
  # ]
  if (use_libbsd) {
    deps += [
      "bsd:drivers"
    ]
    defines += ["__rtems_libbsd__"]
  }
}


# Google testsuite

component("googletest") {
    include_dirs = ["."]
    sources = [
        "src/gtest-all.cc",
        "src/gtest_main.cc"
    ]
    configs = [":googletest_config"]
}

config("googletest_config") {
    include_dirs = [
        "include",
    ]
}